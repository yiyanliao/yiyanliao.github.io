.footer_wrap
    - var now = new Date()
    - var nowYear = now.getFullYear()
    if theme.since && theme.since != nowYear
        .copyright!= `&copy; ${theme.since}–${nowYear} ${config.author}`
    else
        .copyright!= `&copy; ${nowYear} ${config.author}`